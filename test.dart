import 'package:flutter/material.dart';
class widget extends StatelessWidget {
    widget({Key? key}) : super(key);
    @override
    Widget build(BuildContext context) {
        return Container(
        	margin: EdgeInsets.all(8),
        	child: Text('some text'),
        );
    }
}